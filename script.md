# 발표 대본: 에이전트 하네스로서의 코드

> 총 35~40분 구성 (발표 ~36분, 슬라이드 21장 + Q&A 버퍼). 슬라이드별 발표자 노트는 `index.html`에 내장되어 있으며(`S` 키로 팝업), 이 문서가 전체 대본의 통합본입니다.
>
> **데모 영상**: 슬라이드 18의 "🎬 데모 재생" 배지를 클릭하면 도식이 `demo.mp4`(1분 25초)로 전환됩니다. 다시 클릭하면 도식으로 복귀. 글자가 작으므로 재생 직후 영상 우하단 전체화면 버튼을 누르는 것을 권장합니다.
>
> **SkillOpt 섹션(슬라이드 10~14)**: 논문 원본 Figure 1(개요)·Figure 2(파이프라인)를 슬라이드 11·12에 그대로 임베드했습니다. 이 두 장은 그림을 짚어가며 설명하는 슬라이드이므로, 발표 시 마우스 포인터나 레이저로 그림의 각 요소를 가리키며 진행하세요.

---

## 섹션 1 — 오프닝 (~3분)

### 슬라이드 1: 타이틀 [~30초]

안녕하세요. 오늘은 **"에이전트 하네스로서의 코드"**라는 주제로 이야기하겠습니다.

에이전트를 직접 만들지 않으셔도 괜찮습니다. 오늘 발표는 "AI 에이전트가 실제로 어떻게 굴러가는가"를 이해하는 자리이고, 그 중심에 의외로 **코드**가 있다는 이야기입니다.

### 슬라이드 2: 프롬프트에서 하네스로 [~1분 30초]

먼저 이 주제가 어디서 왔는지 위치를 잡겠습니다.

- 2023년엔 다들 "프롬프트를 잘 쓰는 법"을 얘기했습니다 — 프롬프트 엔지니어링.
- 2024년엔 프롬프트 하나가 아니라 모델에게 주는 맥락 전체를 관리하는 **컨텍스트 엔지니어링**으로 넘어왔습니다.
- 지금은 프롬프트와 컨텍스트를 포함해, 모델을 둘러싼 **시스템 전체를 설계하는 "하네스 엔지니어링"** 단계입니다.

오늘 발표는 이 세 번째 단계 이야기입니다. 한 가지만 미리 말씀드리면, 이건 앞 단계를 버리는 게 아니라 **감싸 안는** 흐름입니다 — 프롬프트도 컨텍스트도 여전히 하네스의 한 부품으로 들어갑니다.

### 슬라이드 3: 모델만으로는 안 된다 [~1분 30초]

왜 하네스가 필요할까요? LLM은 본질적으로 **텍스트가 들어가면 텍스트가 나오는 함수**입니다.

- 대화가 끝나면 아무것도 기억하지 못합니다.
- 스스로 코드를 실행할 수 없습니다.
- 최신 정보를 찾아볼 수 없습니다.
- 자기 출력이 맞는지 검증할 수 없습니다.

그래서 이 인용이 핵심입니다 — *"괜찮은 모델 + 훌륭한 하네스가, 훌륭한 모델 + 나쁜 하네스를 이긴다."* (Addy Osmani)

모델 성능 경쟁만 보던 시각에서 벗어나자는 게 오늘의 출발점입니다. 실제로 같은 모델이라도 하네스를 어떻게 짜느냐에 따라 결과가 크게 갈리는데, 그 격차가 어디서 오는지를 지금부터 하나씩 풀겠습니다.

---

## 섹션 2 — 배경: 하네스란? (~9분)

### 슬라이드 4: Agent = Model + Harness [~3분]

(슬라이드 = Addy Osmani의 "회로판" 일러스트) 이 그림이 직관적입니다 — **모델은 보드 위 칩 하나**일 뿐이고, 그 주위를 감싼 게 전부 하네스입니다. 하네스의 가장 간결한 정의는 LangChain의 것이죠 — **"모델이 아닌 모든 것이 하네스다."** 모델 안에 지능이 들어 있다면, **그 지능을 쓸모 있게 만드는 게 하네스**입니다.

에이전트 = 모델 + 하네스. 그림에서 위쪽 컨텍스트 주입(프롬프트·메모리·스킬), 왼쪽 제어(오케스트레이션·루프), 오른쪽 행동(Bash·도구·MCP), 아래쪽 관찰/검증(테스트·로그)과 영속(파일·git) — 모델을 감싸는 이 전체 레이어가 하네스입니다.

조금만 더 구체적으로 들어가 보겠습니다. LangChain은 실전 하네스를 11개 정도의 부품으로 해부하는데, 비개발자 청중을 위해 핵심 세 가지만 짚겠습니다.

- **오케스트레이션 루프** — 흔히 ReAct 또는 TAO(Thought-Action-Observation) 루프라고 부릅니다. "프롬프트를 조립하고 → 모델을 호출하고 → 출력을 파싱해서 → 도구를 실행하고 → 그 결과를 다시 모델에 넣고 → 끝날 때까지 반복"하는 사이클이죠. 모델은 한 번 부르면 한 번 답하고 끝나는데, 이 루프가 그걸 **'끝까지 일하는 엔진'**으로 바꿔 줍니다.
- **도구(Tools)** — 모델에게는 도구가 그냥 "이름·설명·인자 타입"이 적힌 **스키마**로 주어집니다. 하네스는 그 스키마를 등록하고, 모델이 부른 인자를 검증하고, 샌드박스에서 실제로 실행한 뒤, 결과를 다시 모델이 읽을 수 있는 형태로 돌려줍니다.
- **파일시스템** — 의외로 이게 **가장 근본적인 부품**입니다. 파일시스템이 있어야 작업 공간이 생기고, 진행 상황을 조금씩 저장할 수 있고, 여러 에이전트가 같은 파일을 통해 협업할 수 있습니다. 뒤에서 다룰 '상태 보존'의 토대가 바로 이겁니다.

그래서 Claude Code, Codex CLI, Cursor 같은 제품들의 차이가 사실상 **이 하네스의 차이**입니다. 모델은 같은 걸 써도 결과가 다른 이유가 여기 있습니다.

### 슬라이드 5: 하네스는 모델의 빈칸을 채운다 [~2분 30초]

(슬라이드 = Addy Osmani의 "협곡과 다리" 일러스트) 하네스가 왜 그 모양인지는, 모델이 못 하는 것에서 역으로 추론하면 명확합니다. 그림의 왼쪽 절벽이 "모델 혼자 할 수 있는 것"이고, 오른쪽으로 건너가려면 다리가 필요합니다 — **모델 혼자 못 하는 것마다 하네스가 다리를 놓는 것**이죠.

| 모델의 빈칸 | 하네스의 보완 (= 다리) |
|---|---|
| 대화 간 기억 없음 | 파일시스템 + AGENTS.md 같은 메모리 파일 |
| 코드 실행 불가 | Bash + 샌드박스 |
| 실시간 정보 접근 불가 | 웹 검색 + MCP 도구 |
| 컨텍스트 윈도우 한계 | 컴팩션 + 도구 출력 오프로딩 |
| 장기 작업 일관성 붕괴 | 루프 + 계획 + 자기 검증 |

결국 하나하나가 모델이 못 하는 것을 메워주는 장치인 셈입니다.

여기서 Addy Osmani가 강조하는 설계 원칙 하나를 같이 가져가면 좋습니다 — **하네스는 "원하는 동작에서 거꾸로" 설계한다**는 겁니다. 좋은 하네스의 모든 부품은 **여러분이 실제로 관찰한 어떤 실패에 대응**합니다. 에이전트가 자꾸 깨진 코드를 "다 됐다"고 우기면 타입체크 신호를 루프에 역압력으로 꽂고, 40단계짜리 작업에서 길을 잃으면 계획 담당과 실행 담당으로 쪼갭니다. 그는 이걸 **래칫(ratchet) 원리**라고 부릅니다 — 에이전트의 실수 하나하나가 다시는 풀리지 않게 하네스를 한 칸씩 조여 나가는, 한 방향 톱니바퀴라는 거죠. 그래서 잘 굴러간 하네스일수록 **관찰된 실패의 누적 기록**에 가깝습니다.

### 슬라이드 6: 가이드는 예방하고, 센서는 교정한다 [~2분 30초]

한 단계 더 들어가면, Birgitta Böckeler의 **가이드/센서** 구분이 유용합니다. 방금 본 "실패에서 거꾸로 설계한다"를 두 방향으로 나눈 것이라고 보시면 됩니다.

- **가이드 = 사전 제어 (Feedforward)**: AGENTS.md, 프롬프트, 규칙, 도구 스키마 — 행동 **전에** 방향을 잡아주는 것. 아예 잘못된 길로 안 가게 막는 쪽입니다.
- **센서 = 사후 교정 (Feedback)**: 린터, 타입체커, 테스트 — 행동 **결과**를 감지해 피드백을 돌려주는 것. 이미 한 일이 틀렸는지 잡아내는 쪽입니다.

이 둘이 맞물리면, 가이드가 못 막은 실수를 센서가 잡아내고, 그 피드백이 다음 행동을 고치는 **자기 교정 루프**가 됩니다. 사람이 매번 들여다보지 않아도 에이전트가 스스로 "어, 테스트가 깨졌네" 하고 되돌아오는 거죠.

[강조] 여기서 주목할 점: **센서는 전부 코드입니다.** 린터도, 타입체커도, 테스트도 코드예요. 가이드는 자연어로 쓸 수 있지만, "통과냐 실패냐"를 기계적으로 판정하는 센서는 코드일 수밖에 없습니다. 이게 다음 섹션의 복선입니다. 그리고 잠시 후 데모 영상에서 가이드와 센서가 실제로 일하는 장면을 직접 보시게 됩니다.

HumanLayer의 표현대로 *"성공은 조용하고, 실패는 시끄럽게"* 만드는 것이 좋은 하네스 설계입니다 — 잘 되면 아무 말 없이 지나가고, 틀리면 즉시 빨간불이 켜지게 해서, 에이전트의 주의를 실패 쪽으로 끌어당기는 거죠.

---

## 섹션 3 — Thesis: 왜 코드인가? (~6분)

### 슬라이드 7: 코드의 3가지 구조적 우위 [~2분 30초]

오늘의 핵심 주장입니다. 42인 공저 서베이 논문 "Code as Agent Harness"의 핵심 논증이고, 논문 원문 표현 그대로 **executable, verifiable, stateful**입니다.

1. **실행 가능 (Executable)** — 모델의 출력이 그냥 글이 아니라 실제 동작이 됩니다. "이렇게 하면 됩니다"라는 설명이 아니라, 실제로 돌아가는 한 줄의 코드가 되는 거죠.
2. **검증 가능 (Verifiable)** — 통과냐 실패냐를 기계가 판정합니다. 사람이 읽고 "음 괜찮네" 하는 것과 차원이 다릅니다. 주관이 끼어들 여지가 없습니다.
3. **상태 보존 (Stateful)** — 파일과 프로그램은 작업 진행 상황의 영속적 기록이 됩니다. 대화가 끊겨도, 컨텍스트가 날아가도, 파일과 git에는 남아 있습니다.

이 세 단어를 기억해 두세요. 슬라이드 6의 "센서는 전부 코드"였던 이유가 이 중 'Verifiable'이고, 마지막 데모 영상에서 세 가지가 전부 등장합니다.

### 슬라이드 8: 대체가 아닌 역할 분담 [~1분 30초]

자연어와 직접 비교해 보겠습니다. 단, 오해 방지 — **이건 "자연어는 나쁘다"가 아닙니다.** 하네스의 인프라 층, 즉 실행·검증·상태 유지 역할 기준의 비교입니다.

그 기준에서 자연어는 해석이 필요하고, 검증이 주관적이고, 매번 재설명해야 하고, 비결정적입니다. 코드는 전부 반대입니다.

[전환] 그럼 자연어는 쓸모없나? 아닙니다 — **자연어는 다른 층에서 일합니다.** 전략과 휴리스틱을 담는 적응 층이죠. 그 증거가 다음 섹션의 SkillOpt입니다. 코드가 '실행·검증·상태'를 책임지는 인프라라면, 자연어는 그 인프라 위에서 '무엇을 어떻게 할지'를 일러주는 지침이라는, 역할 분담을 잡아 두고 넘어가겠습니다.

### 슬라이드 9: 코드가 하네스가 되는 구체적 형태 [~1분 30초]

추상적인 세 속성의 실제 모습입니다.

- **훅**: 코드로 작성된 규칙의 자동 집행 (Executable)
- **린터/타입체커**: 에이전트 출력을 코드가 검증 (Verifiable)
- **테스트**: "완료"의 정의 자체를 코드로 박아두는 것 (Executable + Verifiable)
- **파일시스템/Git/메모리 파일**: 작업 상태의 영속화 (Stateful)

아까 Böckeler의 "센서"가 전부 코드였던 이유가 바로 이것입니다 — **검증 가능한 피드백은 코드만 만들 수 있습니다.** 이 네 가지가 그대로 다음 섹션의 SkillOpt가 딛고 서는 발판이 되고, 마지막 사례(oh-my-openagent)에서도 똑같은 이름으로 다시 등장합니다.

---

## 섹션 4 — 증거: SkillOpt (~11분)

> 이 섹션은 그림 두 장(Figure 1·2)을 짚어가며 설명하는 게 핵심입니다. 슬라이드 11·12에서는 천천히, 그림의 각 요소를 가리키며 진행하세요.

### 슬라이드 10: SkillOpt 소개 — 스킬 문서를 학습시킨다 [~2분]

먼저 용어 정리부터 — 여기서 말하는 **"스킬"은 에이전트가 읽는 자연어 지침 문서(Markdown)**입니다. `AGENTS.md`, `SKILL.md`처럼 전략·노하우·도메인 지식을 적어둔 것. 슬라이드 8에서 "자연어가 일하는 다른 층"이라고 했던, 바로 그 적응 층의 실체입니다.

Microsoft Research의 SkillOpt — 한 줄 요약: **"모델 가중치는 그대로 두고, 이 스킬 문서를 딥러닝처럼 최적화한다."**

논문이 던지는 문제의식은 이렇습니다. 지금까지 스킬은 ① 사람이 손으로 쓰거나, ② LLM에게 한 번 생성시키거나, ③ 느슨하게 자기 수정을 반복하게 두는 식이었는데 — **그 어느 것도 "옵티마이저처럼" 동작하지 않고, 출발점보다 확실히 나아진다는 보장도 없었다**는 거죠. 그래서 논문은 스킬을 "프롬프트의 부산물"이 아니라, **얼린 에이전트의 외부 상태(external state)로서 제대로 '학습시켜야 할 대상'**으로 보자고 제안합니다.

두 접근의 차이가 큽니다:

| | 모델 파인튜닝 (가중치 갱신) | 스킬 최적화 (외부 텍스트 갱신) |
|---|---|---|
| 비용 | GPU 비용 큼 | 저렴 |
| 모델 교체 | 모델마다 재학습 | 그대로 재사용 |
| 안전성 | catastrophic forgetting 위험 | 가중치 불변 → 안전 |
| 폐쇄형 모델 | 불가 (가중치 접근 필요) | 적용 가능 |

- 굳이 비유하면 **"모델 재교육"이 아니라 "더 나은 업무 매뉴얼 작성"**입니다. 직원(모델)을 다시 가르치는 게 아니라, 같은 직원에게 더 잘 정리된 매뉴얼을 쥐여 주는 쪽이죠. 같은 모델도 매뉴얼이 좋으면 성과가 달라집니다.

### 슬라이드 11: 최적화 루프 — 딥러닝과 똑같은 구조 (Figure 1) [~2분 30초]

**[이 슬라이드는 논문 원본 Figure 1입니다. 그림을 가리키며 설명하세요.]**

AI 팀이시니 이 그림이 제일 빠를 겁니다. **왼쪽은 우리에게 익숙한 손실 곡면(loss landscape)**입니다. 단, 가로 두 축이 모델 파라미터가 아니라 **"스킬 공간(Skill Space)"**이고, 세로축이 **검증 오차(Val. Error)**입니다. 왼쪽 위 문서 아이콘 `S0`가 초깃값 스킬이에요.

- **회색 점선 = ad hoc updates.** 통제 없이 스킬을 막 고쳐 쓰면, 한 번에 의미가 크게 점프하면서(large semantic jumps) 불안정하게 헤매다, 오른쪽의 엉뚱한 골짜기 — **suboptimal skill** — 에 빠집니다. 기존의 "그냥 자기 수정 반복" 방식이 이겁니다.
- **파란 굵은 화살표 = SkillOpt.** 한 걸음 한 걸음이 작게 **제한된 편집(bounded skill edits)**이고, 매 걸음마다 작은 **게이트 아이콘(홀드아웃 검증 게이트)**을 통과해야만 아래로 내려갑니다. 곁가지로 빠지려는 빨간 점선 수정들은 게이트에서 **거부(rejected side updates)**되고요. 그래서 곡면을 안정적으로, 통제된 채로 내려가 제대로 된 최저점에 도달합니다.

이제 **오른쪽 표가 오늘 섹션의 심장**입니다. 신경망 학습의 각 요소가 텍스트 공간에 1:1로 대응됩니다 — 이 대응표만 머리에 넣으시면 SkillOpt는 다 이해하신 겁니다.

| 신경망 학습 | → | SkillOpt (텍스트 공간) |
|---|---|---|
| 파라미터 (parameter) | → | 스킬 문서 |
| 그래디언트 방향 | → | 트래젝토리에서 끌어낸 편집 방향 |
| 학습률 (learning rate) | → | 편집 예산 (한 번에 몇 줄까지 고칠지) |
| 검증 체크 | → | 홀드아웃 선택 게이트 |
| 안정적 학습 세팅 (배치 등) | → | 배치 / 미니배치 / 스케줄 / 게이트 |

그리고 오른쪽 위 작은 그림 보이시죠 — 모델 `f`는 **눈송이(❄), 즉 얼어 있고(frozen)**, 불꽃(🔥) 표시가 붙은 **스킬 `S`만 학습**됩니다. 입력 `x`가 들어가 출력 `y`가 나오는 동안 학습되는 건 오직 그 스킬 문서 하나예요. 결과물은 세 가지 좋은 성질을 갖는 스킬입니다 — **과제 성능 향상, 모델·하네스 간 일반화, 저비용 이식.** 이 세 가지는 슬라이드 13(결과)에서 숫자로 다시 만납니다.

한 줄로 요약하면 — **가중치 대신 텍스트를, 딥러닝과 똑같은 규율(학습률·검증·스케줄)로 학습시킨다.** 이게 SkillOpt입니다.

### 슬라이드 12: 최적화 파이프라인 — 실제 동작 (Figure 2) [~2분 30초]

**[이 슬라이드는 논문 원본 Figure 2입니다. 왼쪽에서 오른쪽으로 한 바퀴 따라가며 짚으세요.]**

방금 건 개념도였고, 이건 **실제 파이프라인**입니다. 한 바퀴 같이 돌아 보죠.

- **왼쪽 위 DATASET — 데이터를 셋으로 나눕니다.** 학습(train) 분할은 롤아웃 증거를 모으고, 검증(validation) 분할은 수정을 받아들일지 결정하는 게이트에 쓰고, 테스트(test) 분할은 **자물쇠(🔒)**가 걸려 최종 보고 전까지 절대 안 엽니다. 이게 중요합니다 — 우리가 보고하는 점수는 학습에도 검증 게이트에도 쓰지 않은, 진짜 처음 보는 데이터 점수예요. 과적합으로 부풀린 숫자가 아니라는 뜻입니다.
- **왼쪽 아래 Fixed Agent — 얼린 타깃 모델.** 그 옆 불꽃 붙은 `S_t`가 지금 학습 중인 스킬 문서입니다.

이제 ①~⑤로 한 바퀴:

1. **롤아웃 (forward pass).** 얼린 모델이 현재 스킬을 들고 과제를 풉니다. 트래젝토리와 점수(그림엔 score 0.463)가 쌓여요. "이 매뉴얼로 일을 시켜 보고 결과를 본다"는 단계입니다.
2. **미니배치 반성 (backward pass).** 그 트래젝토리를 미니배치로 쪼개, **별도의 강한 "옵티마이저 모델"**이 분석합니다. 여기 핵심은 **실패와 성공을 분리해서 본다**는 것. 실패 묶음에선 빠진 규칙을 보충하고, 성공 묶음에선 잘 되던 행동을 보존합니다. 단일 사례가 아니라 묶음을 보기 때문에 *"매번 엉뚱한 출처를 뒤진다", "답 형식을 틀린다"* 같은 **반복되는 절차적 오류**가 드러납니다 — 일회성 땜질이 아니라요.
3. **ADD · DELETE · REPLACE.** 각 미니배치가 추가·삭제·교체 세 종류의 **원자적 편집**을 내놓습니다.
4. **병합 + 예산으로 클립.** 배치 단위로 중복·모순을 정리하고(Batch-level merge), **편집 예산 `L_t`만큼만 랭킹해서 잘라냅니다(Rank/clip).** 이 편집 예산이 바로 **학습률**이에요 — 한 스텝에 몇 개까지만 고치게 막아서, 스킬이 한 번에 와르르 무너지지 않게 합니다. 코사인 스케줄로 처음엔 크게, 갈수록 작게 줄여 가고요.
5. **검증 게이트 (노란 다이아몬드).** 만들어진 후보 스킬 `S_t+1`을 검증셋에 돌려, **점수가 "엄격히 더 높을 때만" 수용(accept)**합니다. 동점도 거부예요. 수용된 것 중 최고가 `best_skill.md`로 빠집니다. *여기가 결정적입니다* — 그럴듯해 보이는 진단도 실제 모델 성능을 떨어뜨릴 수 있으니, 말로 끝내지 않고 **반드시 실측으로 검증**합니다. 슬라이드 6의 '센서'가 여기서도 일하는 거죠.

그리고 두 가지 안정 장치가 더 있습니다:

- **거부된 편집 버퍼 (오른쪽 rejected-edit buffer).** 게이트에서 떨어진 수정도 버리지 않고 저장해서, 다음 반성 때 *"이건 해봤는데 점수 떨어졌다"*는 **음의 피드백**으로 재활용합니다. 같은 실수를 반복하지 않게요.
- **에포크 단위 slow/meta 업데이트 (아래쪽 옅은 박스).** 한 에포크가 끝나면 이전 에포크 스킬과 현재 스킬을 같은 문제들에 돌려, 개선·퇴보·지속 실패·안정 성공으로 분류하고, 장기적으로 유효한 교훈을 **보호된 영역**에 적어 둡니다. 신경망의 **모멘텀** 같은 역할입니다 — 에포크를 넘어 일관된 방향을 끌고 가는 거죠.

정리하면 — 롤아웃, 반성, 제한된 편집, 검증 게이트, 거부 버퍼, slow 업데이트. **딥러닝 학습 루프를 통째로 텍스트 공간에 옮겨 놓은 것**이고, 그래서 논문이 "ad hoc 자기 수정이 아니라 controlled training"이라고 부르는 겁니다.

### 슬라이드 13: 결과 [~2분 30초]

**[막대 읽는 법 먼저]** 연한 칸 = 스킬 적용 전 베이스라인, 진한 칸 = SkillOpt가 더해준 향상분. 막대 끝 = 최종 성능. GPT-5.5 기준, 세 환경에 공통으로 있는 5개 벤치마크 평균입니다.

| 환경 | 베이스라인 | SkillOpt 후 | 향상폭 |
|---|---|---|---|
| Direct chat (하네스 없음) | 53.8 | **79.6** | +25.8 |
| Codex (코드 하네스) | 54.0 | **78.8** | +24.8 |
| Claude Code (코드 하네스) | **57.8 (최고)** | **76.9** | +19.1 |

**[선제 설명 — 오해 차단]** Claude Code의 향상폭(+19.1)이 제일 작은데, 이게 **"Claude Code 하네스가 성능이 낮다"는 뜻이 절대 아닙니다.** 오히려 반대입니다 — Claude Code는 스킬 적용 전 베이스라인이 57.8로 셋 중 가장 높습니다. 출발점이 높으니 올라갈 여지가 작아 향상폭이 작게 나온 것뿐이고, **최종 성능은 76.9~79.6으로 세 환경이 대등**합니다. 즉 향상폭의 크기는 '실력 차이'가 아니라 '남은 여유 공간'의 차이입니다.

핵심 메시지 세 가지로 풀겠습니다:

**1) 환경을 안 가린다 — 평가한 모든 조합에서 1위 또는 공동 1위.** (직접 채팅 7모델 × 6과제 = 42, Codex·Claude Code 하네스는 각 GPT-5.5 × 5과제 = 5 → 합 **52개 조합 전부**.) Human이 손으로 쓴 스킬은 물론, GEPA·TextGrad 같은 프롬프트 최적화, Trace2Skill·EvoSkill 같은 스킬 진화 기법까지 **기존 6개 경쟁 기법을 전부 앞섰습니다.** "셀마다 가장 잘한 놈만 골라 모은" 가상의 오라클 베이스라인보다도 평균 +5.4점 위였고요.

**2) 향상폭이 크다, 특히 절차적 과제에서.** 단순 사실 QA(SearchQA)는 이미 천장에 가까워 +9.6에 그치지만, **엄격한 절차·형식이 필요한 과제일수록 극적**입니다 — SpreadsheetBench 41.8 → **80.7**, OfficeQA 33.1 → **72.1**, LiveMath 37.6 → **66.9**. 그리고 흥미로운 점 — **작고 약한 모델일수록 상대적 이득이 큽니다.** 작은 모델(GPT-5.4-nano)은 DocVQA가 두 배, ALFWorld가 세 배가 됐어요. 가중치 안에 없는 절차 지식을 스킬 문서가 바깥에서 보충해 준다는 해석입니다.

**3) 그런데 결과물은 마크다운 문서 하나.** `best_skill.md`, 300~2,000토큰(중앙값 약 920). 더 놀라운 건 **이 향상이 고작 1~4개의 채택된 편집에서 나온다**는 점입니다 — OfficeQA의 +39.0점, LiveMath의 +29.3점은 **단 한 번의 편집**에서 나왔어요. 옵티마이저가 수백 개를 제안해도 검증 게이트를 통과하는 건 한 줌뿐이라는 뜻이고, 그래서 배포물이 깔끔합니다. **배포 시 추가 추론 비용은 0** — 옵티마이저는 학습 때만 돌고, 배포된 스킬은 타깃 모델만 부릅니다.

**[시간 여유 있으면 — 학습된 스킬이 실제로 뭐라고 적혀 있나]** 추상적으로 들리실 텐데, 논문이 실제 문장을 공개합니다. 예를 들어 SpreadsheetBench 스킬에는 *"워크북 구조와 수식을 먼저 점검하고, Excel 재계산에 기대지 말고 요청된 범위 전체에 평가된 정적 값을 직접 써라"*, LiveMath 스킬에는 *"'가장 강한 명제' 객관식에선 정리의 강도로 보기를 정렬해, 참이지만 약한 따름정리보다 정당화된 더 강한 결과를 골라라"* 같은 규칙이 들어 있습니다. 보시면 **특정 문제에 대한 답이 아니라, 사람 전문가가 그 벤치마크를 하루 만져 보고 적을 법한 '절차적 노하우'**예요. 이게 자동으로, 검증을 통과한 것만 쌓여서 만들어진 겁니다.

**[한 줄 더 — 이식성]** 그리고 이 문서는 학습한 자리를 벗어나도 값을 합니다. 다른 크기 모델로 옮겨도, Codex에서 학습한 걸 Claude Code로 옮겨도(스프레드시트에서 +59.7점), 심지어 비슷한 다른 수학 벤치마크로 옮겨도 플러스였습니다. **한 번 만들어 여러 곳에 재사용**할 수 있다는 거죠.

> 참고: 논문 헤드라인의 "직접 채팅 +23.5"는 ALFWorld 포함 6개 벤치마크 평균. 위 표는 하네스 3종과 **공정 비교를 위해 공통 5개 벤치마크로 통일**해 +25.8로 계산한 값. (출처: SkillOpt 논문 Table 1, GPT-5.5)

### 슬라이드 14: 세 계층의 분리 [~30초 + 강조]

오늘 발표에서 하나만 기억하신다면 이 그림입니다.

- 모델은 **얼려둡니다** (Frozen).
- 코드 하네스는 **인프라** — 실행·검증·상태, 스킬이 "동작"하게 만드는 기반.
- 자연어 스킬은 **적응 층** — 전략·휴리스틱·도메인 지식, SkillOpt가 최적화하는 층.

슬라이드 8에서 "자연어는 다른 층에서 일한다"고 했던 게 이 그림입니다. 그리고 방금 Figure 2에서 본 검증 게이트가 왜 코드 하네스 위에서 가장 잘 도는지도 여기서 보입니다 — **스킬이 "점수가 올랐다"고 말하려면, 그 점수를 기계적으로 매겨 줄 실행·검증 기반(코드 하네스)이 받쳐 줘야** 하니까요. 세 층은 경쟁이 아니라 분업입니다.

---

## 섹션 5 — 사례: oh-my-openagent (~8분 30초, 서브에이전트 슬라이드 + 데모 영상 포함)

### 슬라이드 15: 안에 무엇이 들어있나 [~1분 30초]

제가 즐겨 쓰는 오픈소스 oh-my-openagent입니다. OpenCode 위에 하네스 레이어를 통째로 얹은 도구라 **"oh-my-opencode"로도 불립니다** (데모 영상의 'OpenCode' 터미널이 이것). 별 개수(6만 2천)보다 **안에 뭐가 들었는지**가 핵심입니다.

**전문 에이전트 11종 — 역할 분담** (하나의 봇이 아니라 분업):

- **Prometheus** — 계획: 인터뷰로 검증된 작업 계획 수립
- **Sisyphus** — 오케스트레이터: 위임하고 완료까지 추진
- **Hephaestus** — 심층 실행: 탐색부터 구현까지 자율
- **Oracle** — 아키텍처·디버깅 판단 / **Librarian** — 문서·코드 검색 / **Explore** — 빠른 탐색(grep)

**빌트인 MCP 5종 — 도구**:

- **Exa**(웹 검색) · **Context7**(공식 문서) · **Grep.app**(깃허브 코드 검색) · **ast-grep**(구조 기반 검색, 25개 언어) · **LSP**(정의 이동·참조 찾기·진단)

**라이프사이클 훅 54+ — 자동 검증**: 파일 수정·커밋 등 이벤트마다 자동 발동해 진단·빌드·테스트 같은 코드 품질 게이트를 실행.

이 구성요소들이 앞에서 배운 가이드·센서·오케스트레이션을 그대로 구현합니다 (다음 슬라이드에서 1:1 매핑).

### 슬라이드 16: 이론 → 실제 매핑 [~1분 30초]

앞에서 본 개념이 이름만 붙어서 그대로 들어있습니다.

- 가이드 (사전 제어) → 에이전트 프롬프트 + 계층형 `AGENTS.md` (`/init-deep`로 자동 생성)
- 센서 (사후 교정) → LSP 진단·빌드·테스트 훅 54+ (파일을 고칠 때마다 자동 발동)
- 오케스트레이션 → 11종 에이전트 분업 — **Prometheus**(계획)·**Sisyphus**(실행 지휘)·**Oracle**(아키텍처/디버깅)·**Librarian**(문서/검색)·**Explore**(빠른 탐색) 등
- 상태 보존 → 파일시스템 + Git + Todo 시스템
- (브리지) 자연어 스킬 → `SKILL.md` 스킬 시스템 (playwright·git-master 등) — **4장에서 말한 그 '자연어 스킬'이 여기 실재**

[우측 화면] Team Mode — 에이전트를 최대 8개까지 병렬로 띄워 tmux로 한눈에 보여줍니다. (슬라이드의 실제 스크린샷)

[강조] 새로운 개념이 하나도 없죠? **이론·증거·제품이 한곳에서 만나는 겁니다.**

### 슬라이드 17: 서브에이전트 = 컨텍스트 방화벽 [~1분 30초]

(슬라이드 = HumanLayer의 "Context Firewall" 도식) omo가 왜 에이전트를 11개로 쪼개 두었는지, 그 핵심 이유입니다. 메인 에이전트가 탐색을 직접 하면 Grep·Read·Bash 호출과 결과가 전부 메인 컨텍스트에 쌓여, 수십 번이면 중요한 맥락이 노이즈에 묻혀 성능이 떨어집니다.

- ① 메인은 "스키마 어디 있어?" 같은 **탐색을 서브에이전트에 위임**
- ② 서브에이전트는 **자기만의 컨텍스트 창**에서 Grep·Read·Bash를 수십 번 실행
- ③ 그 중간 **노이즈는 방화벽이 차단** — `"파일은 여기 있습니다"` 같은 깔끔한 결론만 메인으로 복귀
- ④ 메인 컨텍스트가 **끝까지 깨끗** → 더 길고 복잡한 작업 + 여러 서브에이전트 병렬화

→ 앞 슬라이드의 omo **Explore·Oracle·Librarian**이 정확히 이 방화벽 역할입니다. 말로만 하면 와닿지 않으니, 직접 돌아가는 화면을 준비했습니다.

### 슬라이드 18: 동작 흐름 + 데모 영상 [~4분 = 도식 1분 + 영상 1분 25초 + 마무리 1분]

#### (1) 도식 설명 [~1분]

사용자는 `ulw` — Ultra Work — 한 단어만 입력합니다. 그러면:

1. **계획** 단계가 목표를 분해하고
2. **Sisyphus** (오케스트레이터)가 실행을 지휘하며 — Explore(탐색)·Oracle(판단)·Librarian(참조) 서브에이전트를 부리고
3. 작성된 코드는 즉시 **센서** (진단·빌드·테스트)를 통과해야 하며
4. 실패하면 자동 수정 루프가 모든 태스크 완료까지 반복됩니다 (Ralph Loop)

복잡한 작업이면 Prometheus가 먼저 계획서를 만들고, 오늘 데모처럼 작은 버그 수정은 Sisyphus가 바로 받습니다. 실제 장면을 1분 25초로 녹화해 왔습니다.

**[🎬 "데모 재생" 배지 클릭 → 영상 전환, 전체화면 버튼 클릭]**

#### (2) 영상 내레이션 [1분 25초 — 영상 위에 얹는 멘트, 타임스탬프 기준]

> 상황 설정 먼저 말씀드리면: TODO API 프로젝트입니다. 어제 동료가 우선순위 가중치를 숫자 체계로 리팩토링하는 커밋을 올렸고, 오늘 아침 "정렬된 목록이 중요한 일을 맨 아래로 보여준다"는 버그 제보가 들어온 상황입니다.

- **[0:00–0:05] 프롬프트 입력**
  "OpenCode 터미널입니다. 제가 입력한 건 이게 전부입니다 — `ulw 버그 제보가 들어왔어: 정렬된 todo 목록이 중요한 일을 맨 아래로 보여준대. git log를 확인하고, 원인 찾아서 고쳐줘.` 버그 제보를 그대로 전달했을 뿐, 어느 파일이 문제인지도, '테스트를 돌려라'는 말도 안 했습니다."

- **[0:05–0:13] 가이드 주입**
  "`ulw` 한 단어에 화면 가득 규칙이 주입됩니다 — '증거 없이는 아무것도 믿지 마라, 실행하고 검증하고 증거를 보여라', '실패하는 테스트를 지우거나 스킵하지 마라' 같은 것들. 아까 본 **가이드, 사전 제어**가 이겁니다. 그리고 모델이 곧바로 계획을 세웁니다: git log 확인, 원인 추적, 수정."

- **[0:13–0:30] Git으로 용의자 추적 — Stateful**
  "에이전트가 스스로 `git log`와 `git show`를 실행해서, 어제 커밋 — 'Use numeric priority weights' — 이 타입 정의와 테스트를 건드렸다는 걸 찾아냅니다. **Git 히스토리가 작업 상태의 영속적 기록**, 즉 Stateful 속성이 일하는 장면입니다."

- **[0:30–0:45] 원인 진단**
  "이제 코드를 읽고 원인을 특정합니다: 가중치는 high=3으로 뒤집혔는데, 정렬 비교 함수는 여전히 오름차순이라 high가 맨 아래로 가는 거였죠. 흥미로운 건 다음입니다 — 수정 방법이 두 가지(가중치 되돌리기 vs 비교 함수 뒤집기)인데, 에이전트가 **어제 커밋의 의도**('높은 숫자 = 높은 우선순위')를 존중해서 비교 함수를 내림차순으로 바꾸는 쪽을 선택합니다. 그리고 Todo 세 개를 등록합니다: 수정, 테스트, 빌드."

- **[0:45–1:00] 수정 — Executable**
  "수정은 한 줄입니다. diff가 보이시죠 — `a − b`를 `b − a`로. 모델의 출력이 말이 아니라 **실제 동작하는 코드 변경**이 됩니다."

- **[1:00–1:15] 자율 검증 — Verifiable**
  "여기가 오늘의 하이라이트입니다. **제가 시키지 않았는데** 에이전트가 스스로 테스트를 돌립니다 — 2개 통과. 이어서 빌드까지 돌립니다 — 타입 에러 없음. '증거를 보여라'는 가이드가 시켰고, 테스트와 빌드라는 **센서**가 통과/실패를 기계적으로 판정한 겁니다."

- **[1:15–1:25] 요약 보고**
  "마지막으로 before/after 표와 함께 '수정 완료, 테스트 통과, 빌드 성공'을 보고합니다. 전체 비용은 22센트, 컨텍스트는 17%만 썼습니다."

**[영상 종료 → 배지 클릭으로 도식 복귀]**

#### (3) 마무리 멘트 [~1분]

정리하면, 1분 25초짜리 영상 안에 오늘 배운 게 전부 들어있었습니다.

- 규칙 주입 = **가이드** (사전 제어)
- 스스로 돌린 테스트·빌드 = **센서** (사후 교정)
- 코드 수정 = **Executable**, 테스트 판정 = **Verifiable**, Git 히스토리 = **Stateful**

사람이 한 일은 버그 제보를 한 줄 전달한 것뿐이고, 계획-실행-검증 사이클은 **코드 하네스 위에서 자율적으로** 돌았습니다. 그리고 한 가지 더 — 방금 SkillOpt가 검증 게이트로 스킬을 골랐던 그 '실측 검증'이, 여기서는 에이전트가 스스로 돌린 테스트·빌드로 똑같이 일어났습니다. **같은 원리(센서로 검증)가 학습 시점(SkillOpt)과 실행 시점(omo) 양쪽에서** 돌아가는 거죠.

---

## 섹션 6 — 마무리 (~3분 30초)

### 슬라이드 19: Harness-as-a-Service [~1분]

마무리 전에 흐름 하나만 짚겠습니다 — **Harness-as-a-Service, 줄여서 HaaS**입니다.

지금까지 우리는 **LLM API**를 불렀습니다. 넣으면 "완성된 텍스트 한 덩이(completion)"가 나오는 거였죠. 그러면 루프도, 도구 호출도, 대화 상태도, 승인 흐름도 전부 직접 만들어야 했습니다.

그런데 흐름이 바뀌고 있습니다 — 이제는 **"하네스 API", 즉 런타임을 통째로 받아 씁니다.** Claude Agent SDK, Codex SDK, OpenAI Agents SDK가 다 같은 방향이에요. 루프·도구·컨텍스트 관리·훅·샌드박스가 기본으로 들어 있고, 우리는 **네 개 기둥 — 시스템 프롬프트 · 도구 · 컨텍스트 · 서브에이전트** — 만 우리 도메인에 맞게 설정하면 됩니다.

이게 왜 중요하냐면 — 앞에서 'skill issue', 즉 대부분의 실패는 모델이 아니라 **설정 문제**라고 했죠. 매번 에이전트를 밑바닥부터 다시 만들면 그 설정을 손볼 수가 없습니다. **잘 구조화된 런타임 위에서 네 기둥만 튜닝**하니까, 비로소 '관찰한 실패 → 설정 한 줄 추가'라는 **래칫(슬라이드 5)이 현실적으로** 돌아갑니다.

그리고 오늘 본 것들이 전부 이 그림 위에 있습니다 — oh-my-openagent는 OpenCode라는 런타임 위에 하네스 레이어를 얹은 것이고, SkillOpt도 결국 이 런타임들의 **스킬 계층(적응 층)을 최적화**한 것이죠. (개념: Harness-as-a-Service · Addy Osmani 블로그)

### 슬라이드 20: 시사점 3가지 [~1분 30초]

1. **"더 좋은 모델"만이 답이 아니다** — 같은 모델, 하네스만 교체해서 Terminal Bench Top 30 → Top 5.
2. **코드가 최적의 하네스 매체다** — Executable · Verifiable · Stateful.
3. **하네스는 최적화 가능한 대상이다** — 감이 아니라 SkillOpt처럼 체계적으로. 학습률·검증·스케줄을 텍스트 공간에 그대로 들여와, 하네스의 적응 층(스킬)을 딥러닝처럼 훈련시킬 수 있습니다.

마지막 인용: *"하네스는 줄어들지 않고, 이동한다. 모델이 새 천장을 열면, 새로운 하네스가 필요해진다."* — 감사합니다.

### 슬라이드 21: 참고 자료 + Q&A

---

## Q&A 예상 질문 & 답변

**Q. 프롬프트 엔지니어링이랑 뭐가 다르냐?**
프롬프트는 하네스의 한 구성요소일 뿐. 하네스는 실행 환경 + 도구 + 검증 루프까지 포함하는 시스템 전체.

**Q. 자연어 스킬이면 코드 아닌데?**
맞다. 층이 다르다 — 코드는 인프라 층(실행·검증·상태), 자연어 스킬은 적응 층(전략·휴리스틱). 스킬은 하네스의 도구·검증 루프를 "사용하는 방법"을 담는다. 코드 하네스가 그 스킬을 실행·검증해주는 기반이지, "하네스 위라서 절대 성능이 더 높다"는 게 아니다 (논문상 최종 성능은 환경 대등).

**Q. Claude Code에서는 향상폭(+19.1)이 작은데, 하네스가 오히려 방해 아닌가?**
향상폭은 '실력 차이'가 아니라 '남은 여유 공간'이다. Claude Code는 스킬 적용 전 베이스라인이 57.8로 셋 중 가장 높아서(Direct 53.8, Codex 54.0) 올라갈 여지가 작을 뿐, 최종 성능은 76.9~79.6으로 세 환경이 대등하다. 즉 "하네스가 성능을 낮춘다"는 데이터가 아니다. (논문 표, GPT-5.5 공통 5개 벤치마크 평균)

**Q. 그럼 "하네스 위에서 절대 성능이 더 높다"는 건가?**
아니다 — 그렇게까지는 말할 수 없다. 논문 표 기준 최종 절대 성능은 세 환경이 대등(~77–80)하다. 정확한 주장은 "스킬 최적화는 하네스 유무·종류와 무관하게 일관되게 통한다 + SkillOpt가 모든 셀에서 최고"이지, "하네스가 절대 성능을 더 높인다"가 아니다.

**Q. (Figure 1) "텍스트 공간 최적화"가 그냥 비유 아닌가, 진짜 그래디언트가 있나?**
진짜 수치 미분은 아니다. 다만 비유가 '장식'이 아니라 '동작'이라는 게 논문의 강조점이다. 그래디언트 방향 대신 트래젝토리에서 끌어낸 '편집 방향', 학습률 대신 '편집 예산(한 스텝 최대 편집 수 L_t)', 검증 대신 '홀드아웃 게이트'가 실제로 같은 역할을 한다. 핵심은 **bounded + validation-gated** — 한 번에 조금만 고치고, 검증셋 점수가 오를 때만 받아들이는 것. 이게 빠지면(논문 ablation의 'without lr', 'without gate') 그냥 ad hoc 재작성이 되어 성능이 떨어진다.

**Q. (Figure 2) 검증 게이트가 그렇게 중요한가?**
그렇다. 논문에서 게이트는 '엄격히 더 높을 때만' 수용(동점도 거부)하도록 보수적으로 설계됐다. 이유는 "그럴듯한 텍스트 진단도 실제 타깃 모델 성능은 떨어뜨릴 수 있다"는 것. 그리고 SpreadsheetBench의 +39점 같은 큰 향상이 **단 1번의 채택 편집**에서 나온다는 게 게이트가 일하고 있다는 증거다 — 옵티마이저가 제안한 대부분은 거부되고, 통과한 소수만 배포 스킬에 남는다.

**Q. 옵티마이저 모델이 강하니까 이긴 것 아닌가? (그냥 distillation 아닌가)**
아니다. 논문이 이걸 직접 실험했다. 강한 프런티어 옵티마이저(GPT-5.5)가 더 큰 향상을 주는 건 맞지만, 타깃과 같은 크기의 옵티마이저로 바꿔도 강한 옵티마이저 이득의 56~74%를 회복한다. 즉 강한 교사로부터의 증류가 아니라, **최적화 루프 자체(제한된 편집·게이트·버퍼·slow update)가 값을 만든다.** 게다가 옵티마이저는 학습 때만 돌고 배포 비용은 0이라, 가능하면 강한 옵티마이저를 쓰는 게 합리적이다.

**Q. 그냥 파인튜닝하면 안 되나?**
폐쇄형 프런티어 모델은 가중치를 못 연다. 열 수 있어도 GPU 비용·catastrophic forgetting·모델마다 재학습 문제가 있다. SkillOpt 산출물은 마크다운 1개라 모델을 갈아타도 재사용되고, 사람이 몇 분이면 읽고 감사·수정할 수 있다. 단, 한계도 분명하다(아래).

**Q. SkillOpt의 한계는?**
논문이 명시한다. ① 점수화된 트래젝토리와 홀드아웃 검증이 필요하므로 **자동 검증기·정답 매칭·실행 체크 같은 신뢰할 신호가 있는 과제에 가장 잘 맞는다.** 성공이 주관적인 열린 과제는 게이트에 사람/모델 평가가 더 필요하다. ② 학습엔 롤아웃·옵티마이저 호출 비용이 든다(배포가 아니라 학습 시점). 일회성 과제엔 덜 매력적. ③ 단일 스킬이라 매우 이질적인 도메인엔 부족할 수 있다.

**Q. 데모에서 에이전트가 테스트를 먼저 안 돌리고 코드부터 읽던데?**
좋은 관찰. 에이전트가 git log에서 바로 용의 커밋을 찾아서 코드 분석으로 직행했다. 진단 경로는 비결정적이지만, **마지막에 테스트·빌드로 증명하는 것은 가이드가 강제**하므로 결정적이다 — "성공은 조용하고 실패는 시끄럽게"의 실례.

**Q. 데모 비용/시간은?**
1태스크 버그 수정 기준 약 $0.22, 컨텍스트 17% 사용, 실시간 약 1분 30초 (영상은 무편집).

**Q. 우리 팀에 당장 적용할 수 있는 건?**
① AGENTS.md 작성 (60줄 이하, '관찰한 실패에서 거꾸로') ② 린터/타입체커를 에이전트 피드백으로 연결(센서) ③ 스킬을 분리하여 점진적 공개. ④ 검증 신호(테스트/정답 매칭)가 있는 반복 과제라면 SkillOpt식 루프(돌려보고 → 실패 분석 → 스킬 한 줄 추가 → 검증 통과 시만 채택)를 수동으로라도 흉내 낼 수 있다.

**Q. 모델이 더 좋아지면 하네스 필요없지 않나?**
"하네스는 줄지 않고 이동한다" — 새 모델이 옛 하네스를 불필요하게 만들지만, 새 천장이 열리며 새 하네스가 필요해진다.

**Q. oh-my-openagent 말고 다른 하네스는?**
Claude Code, Codex CLI, Cursor, Aider, Cline 모두 하네스. 모델은 같아도 하네스가 달라서 결과가 다르다.

---

## 데모 영상 정보 (demo.mp4, 1분 25초)

슬라이드 18 임베드. 배지 클릭 → 영상, 다시 클릭 → 도식 복귀. 다른 슬라이드로 이동하면 자동 일시정지. 인쇄(PDF)에는 도식만 출력.

| 구간 | 화면 내용 | 발표 포인트 |
|---|---|---|
| 0:00–0:05 | OpenCode + `ulw` 버그 제보 프롬프트 입력 | 사람의 입력은 이 한 줄이 전부 |
| 0:05–0:13 | Ultra Work 규칙 주입, 계획 수립 | 가이드 (Feedforward) |
| 0:13–0:30 | git log/show로 어제 커밋(726a1e9) 추적 | Stateful — Git이 상태 기록 |
| 0:30–0:45 | 원인 진단 (가중치 역전 vs 오름차순 비교자), 수정 방안 선택, Todo 등록 | 커밋 의도를 존중한 판단 |
| 0:45–1:00 | routes.ts 한 줄 수정 diff (a−b → b−a) | Executable |
| 1:00–1:15 | 자율 테스트 (2 passed) + 빌드 (tsc 성공) | **하이라이트** — 센서/Verifiable |
| 1:15–1:25 | 요약 표 + 완료 보고 ($0.22, 컨텍스트 17%) | 비용 어필 |

### 재녹화가 필요할 때 (런북 요약)

- 리셋: `cd ~/demo/todo-api && git reset --hard 726a1e9 && git clean -fd` (커밋 `726a1e9` = PRIORITY_ORDER 역전 버그가 심어진 시작 상태)
- 프롬프트: `ulw 버그 제보가 들어왔어: 정렬된 todo 목록이 중요한 일을 맨 아래로 보여준대. git log를 확인하고, 원인 찾아서 고쳐줘`
- 프롬프트에 "테스트"를 넣지 않는 것이 의도 — 에이전트가 스스로 검증하는 모습이 핵심 메시지
- 데모 repo 안에 런북/스포일러 문서를 넣지 말 것 (에이전트가 읽고 발설할 수 있음)
- 녹화 후 임베드: `ffmpeg -i 원본.mov -c copy -movflags +faststart demo.mp4`

## 발표 전 체크리스트

- [x] `ulw` 실행 장면 녹화 → demo.mp4 (1:25), 슬라이드 18에 토글 임베드 완료
- [x] SkillOpt 논문 Figure 1·2를 슬라이드 11·12에 원본 그대로 임베드 (`assets/skillopt-fig1-overview.png`, `assets/skillopt-fig2-pipeline.png`) — 출처는 슬라이드 하단 `.src`에 표기
- [ ] 슬라이드 11·12는 그림을 짚어가며 설명 — 발표 PC에서 그림 글자 가독성 확인(특히 Figure 2 파이프라인 라벨). 뒷자리에서 안 보이면 해당 슬라이드만 확대 표시 고려
- [ ] oh-my-openagent 스타 수 갱신 (슬라이드 15 카드 = 62k, 2026-06-12 깃허브 기준 — 발표 직전 재확인)
- [ ] 다운로드 수(2.9M+)는 npm 페이지에서 확인되면 슬라이드 15에 카드 추가
- [ ] 발표 PC에 **presentation 폴더째** 복사 — index.html이 `demo.mp4` + `assets/`(harness-anatomy·harness-bridges·context-firewall·omo-opt·sub-agents·skillopt-fig1·skillopt-fig2) 참조, 단일 파일 아님
- [ ] 슬라이드 4·5 도식 = Addy Osmani 블로그 일러스트, 슬라이드 11·12 = SkillOpt 논문 Figure (슬라이드 하단 + 21쪽 참고자료에 출처 표기됨). 사내 교육용 인용
- [ ] 발표 PC에서 index.html 렌더링·폰트 로딩·영상 재생 확인 (인터넷 필요: 폰트 CDN)
- [ ] `S` 키 발표자 노트 팝업이 팝업 차단에 걸리지 않는지 확인
- [ ] 영상 0:25~0:35 구간 `git show`에 커미터 이름/이메일(heegon@Mac.kornet) 노출 — 공개 범위 괜찮은지 한 번 확인
