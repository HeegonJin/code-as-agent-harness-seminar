# 발표 기획서: "에이전트 하네스로서의 코드"

## 1. 기본 정보

| 항목 | 내용 |
|---|---|
| **주제** | 에이전트 하네스로서의 코드 (Code as Agent Harness) |
| **Thesis** | 코드는 자연어보다 에이전트 하네스의 매체로서 구조적 우위가 있다 (실행 가능·검증 가능·상태 보존) |
| **청중** | AI 관련 팀 근무자, 에이전트 개발 비전공 |
| **발표 시간** | 30분 |
| **형식** | HTML/CSS/JS — PPT처럼 보이는 16:9 고정 슬라이드 덱, 다크 모드 (네이비/블랙), 한국어 위주 + 영어 인용 |
| **시각 자료** | 슬라이드마다 내용에 부합하는 figure (SVG 다이어그램) |

---

## 2. 소스 & 역할 배정

| 소스 | 발표에서의 역할 | 사용 위치 |
|---|---|---|
| 진화 프레이밍 (여러 소스 종합) | 배경 위치 설정 | 오프닝 |
| LangChain "Anatomy of an Agent Harness" | 하네스 정의 + 구성요소 | 배경 |
| Addy Osmani "Agent Harness Engineering" | "같은 모델, 다른 하네스 → 다른 성능" 동기 부여 | 배경 |
| Böckeler / Martin Fowler "Harness Engineering" | 가이드 vs 센서 — **센서가 코드**라는 점 강조 | Thesis 보강 |
| Code as Agent Harness 서베이 (Ning et al.) | 코드의 3가지 구조적 우위 (핵심 논증) | Thesis |
| SkillOpt (Microsoft Research) | 코드 하네스 안에서 NL 스킬이 증폭됨 | 증거 |
| oh-my-openagent | 코드-하네스의 완성형 구현체 | 사례 |

### 소스 상세

- **LangChain**: https://www.langchain.com/blog/the-anatomy-of-an-agent-harness
- **Addy Osmani**: https://addyosmani.com/blog/agent-harness-engineering/
- **Böckeler / Martin Fowler**: https://martinfowler.com/articles/harness-engineering.html
- **Code as Agent Harness 서베이**: https://arxiv.org/abs/2605.18747 (Ning et al., May 2026)
- **SkillOpt**: https://arxiv.org/abs/2605.23904 / https://github.com/microsoft/SkillOpt
- **oh-my-openagent**: https://omo.dev / https://github.com/code-yeongyu/oh-my-openagent

---

## 3. 발표 흐름

| # | 섹션 | 시간 | 핵심 메시지 |
|---|---|---|---|
| 1 | 오프닝 | ~3분 | "모델만으로는 에이전트가 안 된다" |
| 2 | 배경: 하네스란? | ~6분 | Agent = Model + Harness, 하네스의 역할 |
| 3 | Thesis: 왜 코드인가? | ~5분 | 코드의 구조적 우위 3가지 (코드 = 인프라 층, 자연어 = 적응 층) |
| 4 | 증거: SkillOpt | ~6분 | 코드 하네스 + NL 스킬 최적화의 시너지 |
| 5 | 사례: oh-my-openagent | ~8분 | 실제 코드-하네스 시스템 (녹화 데모 포함) |
| 6 | 마무리 | ~2분 | 시사점 + "하네스는 이동한다" |

> 시간 배분 원칙: 청중이 에이전트 비개발자이므로 개념(배경)과 체감(사례)에 비중을 두고, SkillOpt는 핵심 루프와 결과만 전달한다.

---

## 4. 슬라이드별 상세 계획

### 섹션 1: 오프닝 (~3분)

#### 슬라이드 1 — 타이틀

- **제목**: 에이전트 하네스로서의 코드
- **부제**: Toward Executable, Verifiable, and Stateful Agent Systems
- **Figure**: 없음 (타이틀 디자인만)

#### 슬라이드 2 — 진화: Prompt → Context → Harness

- **핵심**: "하네스 엔지니어링"의 계보 — 프롬프트 엔지니어링의 다음 단계
- **Figure**: 수평 타임라인 다이어그램
  ```
  [2023]                    [2024]                     [2025-26]
  Prompt Engineering  →  Context Engineering  →  Harness Engineering
  "프롬프트를 잘 쓰자"    "맥락을 관리하자"       "시스템 전체를 설계하자"
  ```

#### 슬라이드 3 — 문제 제기: 모델만으로는 안 된다

- **핵심**: LLM은 텍스트 in → 텍스트 out일 뿐. 상태 없음, 도구 실행 불가, 메모리 없음.
- **인용**: *"A decent model with a great harness beats a great model with a bad harness."* — Addy Osmani
- **Figure**: 모델을 중심에 놓고, 빠져 있는 기능들 (상태, 도구, 메모리, 검증)이 ❌ 표시로 둘러싼 도식

---

### 섹션 2: 배경 — 하네스란? (~5분)

#### 슬라이드 4 — Agent = Model + Harness

- **핵심 공식**: `Agent = Model + Harness`
- **정의**: "모델이 아닌 모든 것이 하네스다" — LangChain
- **구성요소**: System Prompt, Tools/MCPs, Sandbox, Orchestration Logic, Hooks/Middleware
- **Figure**: 중앙에 "Model" 원, 그 주변을 감싸는 동심원/레이어가 Harness의 각 구성요소를 표현

#### 슬라이드 5 — 모델 한계 → 하네스 해결

- **핵심**: 하네스가 존재하는 이유를 모델의 불능(不能)에서 역추론
- **Figure**: 좌우 2열 매핑 다이어그램 (화살표로 연결)

  | 모델이 못하는 것 | 하네스가 제공 |
  |---|---|
  | 대화 간 상태 유지 ❌ | 파일시스템 + 메모리 (AGENTS.md) |
  | 코드 실행 ❌ | Bash + 샌드박스 |
  | 실시간 정보 접근 ❌ | 웹 검색 + MCP 도구 |
  | 컨텍스트 윈도우 한계 ❌ | 컴팩션 + 도구 출력 오프로딩 |
  | 장기 작업 일관성 ❌ | 루프 + 계획 + 자기 검증 |

#### 슬라이드 6 — 가이드 vs 센서 (Böckeler)

- **핵심**: 하네스의 두 가지 작동 원리 — 사전 제어(Feedforward) + 사후 교정(Feedback)
- **Figure**: 가운데 "Agent 행동"을 중심으로 좌측에서 가이드가 들어오고, 우측에서 센서가 피드백 루프를 형성하는 다이어그램
  ```
  [가이드 Guides]         [에이전트 행동]         [센서 Sensors]
   Feedforward ─────→    실행 / 생성    ─────→  Feedback
   (예방: AGENTS.md,      (코드 작성,            (감지+수정: 린터,
    프롬프트, 규칙)        파일 수정)             타입체커, 테스트)
                              ↑                       │
                              └───── 자기 교정 ←──────┘
  ```
- **부연**: "Success is silent, failures are verbose" — HumanLayer

---

### 섹션 3: Thesis — 왜 코드인가? (~5분)

#### 슬라이드 7 — 코드의 3가지 구조적 우위

- **핵심**: 코드가 자연어보다 하네스 매체로서 우월한 근본 이유 (서베이 핵심 논증)
- **출처**: Ning et al., "Code as Agent Harness", arXiv:2605.18747
- **용어 주의**: 논문 원문 표현은 "executable, **verifiable**, and stateful" — 발표 전체에서 이 표현으로 통일 (Inspectable 아님)
- **Figure**: 3개 속성을 아이콘(▶️ ✅ 💾) + 설명으로 가로 배치

  | 속성 | 의미 | 하네스에서의 역할 |
  |---|---|---|
  | **Executable** (실행 가능) | 모델 출력이 실제 동작이 된다 | 의도가 실제 행동으로 검증됨 |
  | **Verifiable** (검증 가능) | 성공/실패를 기계적으로 판정 가능 | 실패 진단 + 피드백 가능 |
  | **Stateful** (상태 보존) | 프로그램 = 작업 진행의 영속적 표현 | 단계 간 정보 유지 |

#### 슬라이드 8 — 코드와 자연어: 대체가 아닌 역할 분담

- **핵심**: "코드가 자연어를 대체한다"가 아니라 **역할이 다르다** — 코드는 인프라 층(실행·검증·상태), 자연어는 적응 층(전략·휴리스틱). 이 프레임이 섹션 4(NL 스킬 최적화)로 자연스럽게 이어진다.
- **주의**: 적대적 비교(자연어 ❌)로 보이지 않게 할 것. 섹션 4에서 자연어 스킬을 찬양할 때 모순으로 느껴지지 않도록, 여기서 "인프라 역할 기준"의 비교임을 명시.
- **Figure**: 비교 테이블 (✅/❌ 아이콘으로 시각 강조) — 제목을 "하네스의 **인프라 층**으로서 비교하면"으로

  | 기준 (인프라 층 역할) | 자연어 프롬프트 | 코드 |
  |---|---|---|
  | 실행 가능? | ❌ 해석 필요 | ✅ 직접 실행 |
  | 검증 가능? | ❌ 주관적 판단 | ✅ 통과/실패 명확 |
  | 상태 보존? | ❌ 매번 재설명 | ✅ 파일/DB/git |
  | 재현 가능? | ❌ 비결정적 | ✅ 결정적 |
  | 조합 가능? | △ 길어지면 충돌 | ✅ 모듈화 |

- **전환 멘트**: "그럼 자연어는 쓸모없나? 아니다 — 자연어는 다른 층에서 일한다. 그 증거가 다음 섹션의 SkillOpt."

#### 슬라이드 9 — 코드가 하네스가 되는 구체적 형태

- **핵심**: 추상적 속성이 실제로 어떤 모습으로 나타나는가
- **연결**: "Böckeler의 '센서'가 전부 코드인 이유가 바로 이것"
- **Figure**: 4칸 아이콘 그리드

  | 형태 | 역할 | 코드 하네스 속성 |
  |---|---|---|
  | **훅** (pre-commit, post-edit) | 코드로 작성된 규칙의 자동 집행 | Executable |
  | **린터 / 타입체커** | 코드가 에이전트 출력을 검증 | Verifiable |
  | **테스트** | 코드가 "완료 조건"을 정의 | Executable + Verifiable |
  | **파일시스템 / Git / 메모리 파일** | 작업 진행 상태가 코드·파일로 영속화 | Stateful |

- **부연 (입으로만)**: 도구 스키마도 코드다 — 에이전트의 행동 공간을 계약으로 제약하는 가이드 역할 (슬라이드에는 넣지 않음)

---

### 섹션 4: 증거 — SkillOpt (~8분)

#### 슬라이드 10 — SkillOpt 소개

- **핵심**: "모델 가중치를 건드리지 않고, 스킬 문서를 딥러닝처럼 최적화"
- **출처**: Microsoft Research, arXiv:2605.23904 (May 2026)
- **비유**:
  - 모델 파인튜닝 = 뇌 수술 (비싸고 위험)
  - 스킬 최적화 = 더 좋은 매뉴얼 작성 (저렴하고 재사용 가능)
- **Figure**: 좌우 대비 — 왼쪽에 "뇌(모델) + 메스" 아이콘, 오른쪽에 "문서 + 연필" 아이콘. 오른쪽이 강조됨.

#### 슬라이드 11 — SkillOpt 최적화 루프

- **핵심**: SkillOpt가 어떻게 작동하는가
- **Figure**: 순환 다이어그램 (4단계 루프)
  ```
  ┌─────────────────────────────────────────┐
  │                                         │
  │    ① 에이전트 실행 (Rollout)            │
  │         ↓                               │
  │    ② 결과 채점 (Scoring)               │
  │         ↓                               │
  │    ③ 옵티마이저가 스킬 수정 제안        │
  │       (Bounded Edit)                    │
  │         ↓                               │
  │    ④ 검증 통과 시만 적용               │
  │       (Validation Gate)                 │
  │         │                               │
  │         └──── 반복 (Epoch) ────→ ①     │
  └─────────────────────────────────────────┘

  ```
- **부연**: "Neural network 학습과 동일한 구조: forward pass → backward pass → update → validate" — AI 팀 청중에게 가장 잘 통하는 비유이므로 이 한 줄을 중심으로 설명
- **안정 장치는 한 줄로만**: "학습률·검증셋 같은 과적합 방지 장치도 텍스트 공간에 그대로 옮겨놨다" (Textual Learning Rate / Rejected-Edit Buffer / Held-out Validation은 슬라이드에 작게 각주 처리, 상세 설명 생략)

#### 슬라이드 12 — SkillOpt 결과

- **핵심**: 숫자로 증명 — 스킬 최적화는 하네스 유무·종류와 무관하게 일관되게 작동하며, **최종 절대 성능은 하네스 위에서 가장 높다**
- **프레이밍 주의**: "향상폭(델타)"이 아니라 "절대 성능"으로 말할 것. 델타만 비교하면 Claude Code(+19.1)가 Direct(+23.5)보다 작아 주장과 모순돼 보임. 실제로는 하네스 베이스라인이 이미 높아서 향상 여지가 작은 것 — **이 점을 발표에서 선제적으로 짚고 넘어갈 것** (질문 받기 전에)
- **Figure**: 바 차트 (3개 막대) — 가능하면 베이스라인+향상폭을 누적 막대로 표현해 절대 성능이 보이게
  ```
  GPT-5.5 기준 평균 정확도 향상 (스킬 적용 전후):

  Direct chat (하네스 없음):     ████████████████████████  +23.5점
  Codex 하네스 안에서:           █████████████████████████ +24.8점
  Claude Code 하네스 안에서:     ████████████████████      +19.1점
  (Claude Code는 베이스라인이 높아 향상 여지가 작음 — 각주로 명시)
  ```
- **추가 수치**:
  - 52개 (모델 × 벤치마크 × 하네스) 셀 → **전부 1위 또는 동률**
  - 결과물: `best_skill.md` 하나 (300~2,000 토큰)
  - 추론 비용 추가: **0** (배포 시 모델 호출 없음)
- **강조**: "어떤 환경에서든 스킬이 통한다 + 하네스 위에서의 최종 성능이 가장 높다 → 코드 하네스는 스킬의 실행 기반"

#### 슬라이드 13 — 핵심 통찰: 코드 하네스 + NL 스킬 = 최적 조합

- **핵심**: NL 스킬은 강력하지만, 코드 하네스 안에서 실행될 때 비로소 최대 효과
- **Figure**: 3계층 스택 다이어그램
  ```
  ┌─────────────────────────────────────┐
  │  NL 스킬 (유연한 지시)              │  ← SkillOpt가 최적화하는 층
  │  "전략, 휴리스틱, 도메인 지식"       │
  ├─────────────────────────────────────┤
  │  코드 하네스 (실행/검증/상태)        │  ← 이 층이 있어야 스킬이 "동작"
  │  "훅, 도구, 샌드박스, 피드백 루프"   │
  ├─────────────────────────────────────┤
  │  모델 (추론 엔진)                   │  ← Frozen, 건드리지 않음
  │  "GPT-5.5, Claude, Qwen..."        │
  └─────────────────────────────────────┘
  ```
- **메시지**: "모델은 고정, 코드 하네스는 인프라, NL 스킬은 적응 계층. 세 계층의 분리가 핵심."

---

### 섹션 5: 사례 — oh-my-openagent (~7분)

#### 슬라이드 14 — oh-my-openagent 소개

- **핵심**: "코드-하네스 개념의 실제 구현체"
- **수치**: 61.9k GitHub Stars (2026-06-11 확인), 11개 전문 에이전트, 54+ 라이프사이클 훅
  - ⚠️ 다운로드 수(2.9M+)는 README에서 미확인 — 발표 전 npm 페이지에서 출처 확보 후 기입, 못 찾으면 카드에서 제외
  - ⚠️ 스타 수는 발표 직전 갱신
- **태그라인**: "the one and only agent harness for complex codebases"
- **Figure**: 로고 + 핵심 수치 카드 (Stars, Agents, Hooks + 확인되면 Downloads)

#### 슬라이드 15 — 아키텍처: 이론 → 실제 매핑

- **핵심**: 앞서 설명한 하네스 개념들이 실제로 어떻게 구현되어 있는지 — **앞 슬라이드에서 이미 등장한 개념과 1:1로만 매핑** (청중이 에이전트 비개발자이므로 새 용어 도입 금지)
- **Figure**: 좌우 2열 매핑 다이어그램 (이론 → 구현), 4행으로 압축

  | 하네스 개념 (앞 섹션에서 설명한 것) | oh-my-openagent 구현 |
  |---|---|
  | 가이드 (Feedforward) | 에이전트 프롬프트 + AGENTS.md |
  | 센서 (Feedback) | 코드 진단·빌드·테스트 훅 (54+ 라이프사이클 훅) |
  | 오케스트레이션 | 11개 전문 에이전트의 분업 (계획 → 실행 → 검증) |
  | 상태 보존 (Stateful) | 파일시스템 + Git + Todo 시스템 |

- **입으로만 보충 (슬라이드에 안 넣음)**: LSP/AST-Grep/MCP/Ralph Loop/Progressive Disclosure 같은 세부 용어는 질문 나올 때만. 에이전트 이름(Sisyphus, Prometheus 등)은 다음 슬라이드 흐름도에서 자연스럽게 소개

#### 슬라이드 16 — 실제 동작 흐름 (+ 녹화 데모)

- **핵심**: `ulw` (Ultra Work) 한 단어로 전체 코드-하네스 파이프라인이 시동
- **데모 (권장)**: `ulw` 실행 후 에이전트들이 계획→탐색→구현→검증을 도는 모습을 **1~2분 녹화 영상**으로 준비 (라이브 데모는 위험하니 녹화로). 비개발자 청중에게는 정적 다이어그램보다 실제로 도는 모습이 훨씬 와닿음. 영상은 HTML에 `<video>`로 임베드하고, 아래 다이어그램은 영상 전 구조 설명용 또는 영상 재생 실패 시 대체재
- **Figure**: 세로 시퀀스/플로우 다이어그램
  ```
  사용자 입력: "ulw"
       │
       ▼
  ┌─ Prometheus (계획 에이전트) ──────────────────────┐
  │  목표 분해, BUILD_PLAN.md 생성                    │
  └───────────────────────────────────────────────────┘
       │
       ▼
  ┌─ Sisyphus (실행 오케스트레이터) ──────────────────┐
  │                                                   │
  │  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
  │  │ Explore  │  │ Oracle   │  │ Librarian│       │
  │  │(탐색)    │  │(판단)    │  │(참조)    │       │
  │  └──────────┘  └──────────┘  └──────────┘       │
  │       │              │              │             │
  │       └──────────────┴──────────────┘             │
  │                      │                            │
  │                      ▼                            │
  │              구현 (코드 작성)                      │
  │                      │                            │
  │                      ▼                            │
  │          ┌─ 센서: lsp_diagnostics ─┐              │
  │          │  빌드 검증, 테스트 실행  │              │
  │          └─────────────────────────┘              │
  │                      │                            │
  │              Pass? ──┼── Fail?                    │
  │               │      │      │                    │
  │               ▼      │      └→ 자동 수정 루프     │
  │            완료      │                            │
  └───────────────────────────────────────────────────┘
       │
       ▼ (모든 태스크 완료까지 Ralph Loop 반복)
  ```

---

### 섹션 6: 마무리 (~2분)

#### 슬라이드 17 — 시사점 3가지

- **Figure**: 3개 카드 가로 배치

  | # | 시사점 | 근거 |
  |---|---|---|
  | 1 | **"더 좋은 모델"만이 답이 아니다** | 동일 모델에서 하네스만 바꿔 Top 30 → Top 5 (Terminal Bench) |
  | 2 | **코드가 최적의 하네스 매체다** | Executable · Verifiable · Stateful |
  | 3 | **하네스는 최적화 가능한 대상이다** | SkillOpt: 체계적 텍스트-공간 최적화 |

- **마무리 인용**: *"하네스는 줄어들지 않고, 이동한다. 모델이 새 천장을 열면, 새로운 하네스가 필요해진다."* — Addy Osmani

#### 슬라이드 18 — 참고 자료

- Figure: 없음 (링크 목록)
- 논문:
  - Ning et al., "Code as Agent Harness", arXiv:2605.18747 (2026)
  - Yang et al., "SkillOpt", arXiv:2605.23904 (2026)
- 블로그:
  - LangChain, "The Anatomy of an Agent Harness" (2026.03)
  - Addy Osmani, "Agent Harness Engineering" (2026.04)
  - Böckeler, "Harness engineering for coding agent users" (2026.04)
  - HumanLayer, "Skill Issue: Harness Engineering for Coding Agents" (2026.03)
- 도구:
  - oh-my-openagent: https://omo.dev

---

## 5. 디자인 명세

| 항목 | 명세 |
|---|---|
| **배경** | 다크 네이비/블랙 (#0a0e27 ~ #1a1f3a) |
| **강조색** | 블루 (#4fc3f7), 퍼플 (#7c4dff), 그린 포인트 (#66bb6a) |
| **텍스트** | 흰색 (#e0e0e0), 밝은 회색 (#b0b0b0) |
| **폰트 (본문)** | Pretendard 또는 Noto Sans KR |
| **폰트 (코드/인용)** | JetBrains Mono |
| **Figure 스타일** | SVG 인라인, 라인/노드 다이어그램 위주, 미니멀, 네온 글로우 효과 자제 |
| **슬라이드 프레임워크** | Reveal.js (CDN) 또는 커스텀 JS |
| **화면비 / 조작** | PPT처럼 보이고 동작할 것 — 16:9 고정 비율, 한 화면 = 한 슬라이드 (스크롤 없음), 방향키/클릭으로 넘김, 슬라이드 번호 표시 (예: 7 / 18) |
| **전환 효과** | 간결 (fade 위주) |
| **파일 구조** | 단일 `index.html` (CSS/JS 임베디드) |

---

## 6. 발표 포인트 & Q&A 대비

| 예상 질문 | 준비할 답변 |
|---|---|
| "프롬프트 엔지니어링이랑 뭐가 다르냐?" | 프롬프트는 하네스의 한 구성요소일 뿐. 하네스는 실행 환경 + 도구 + 검증 루프까지 포함하는 시스템 전체 |
| "NL 스킬이면 코드 아닌데?" | 맞다. 코드와 자연어는 층이 다르다 — 코드는 인프라 층(실행·검증·상태), NL 스킬은 적응 층(전략·휴리스틱). 스킬은 코드 하네스의 도구·검증 루프를 "사용하는 방법"을 담으므로, 하네스 위에서 실행될 때 최종 절대 성능이 가장 높다 |
| "Claude Code에서는 향상폭(+19.1)이 하네스 없을 때(+23.5)보다 작은데, 하네스가 오히려 방해 아닌가?" | 향상폭과 절대 성능을 구분해야 한다. 하네스가 있으면 스킬 적용 전 베이스라인 자체가 이미 높아서 올라갈 여지가 작다. 스킬 적용 후 최종 절대 성능은 하네스 위가 더 높다 |
| "우리 팀에 당장 적용할 수 있는 건?" | ① AGENTS.md 작성 (60줄 이하), ② 린터/타입체커를 에이전트 피드백으로 연결, ③ 스킬을 분리하여 점진적 공개 |
| "모델이 더 좋아지면 하네스 필요없지 않나?" | "하네스는 줄지 않고 이동한다" — 새 모델이 옛 하네스를 불필요하게 만들지만, 새 천장이 열리며 새 하네스가 필요해짐 |
| "oh-my-openagent 말고 다른 하네스는?" | Claude Code, Codex CLI, Cursor, Aider, Cline 모두 하네스. 모델은 같아도 하네스가 달라서 결과가 다름 |

---

## 7. 구현 요구사항 (핸드오프용)

```
## 작업
이 기획서를 기반으로 프레젠테이션 HTML을 구현할 것.

## 구현 스펙
- 단일 index.html (CSS/JS 인라인)
- Reveal.js CDN 사용
- PPT처럼 동작: 16:9 고정 비율, 한 화면 = 한 슬라이드 (내부 스크롤 금지), 방향키/클릭 넘김, 슬라이드 번호 표시
- 다크 테마 (배경 #0a0e27, 강조색 블루 #4fc3f7 / 퍼플 #7c4dff / 그린 #66bb6a)
- 각 슬라이드마다 인라인 SVG figure (섹션 4의 Figure 설명 참고)
- 용어: 코드 속성은 Executable / Verifiable / Stateful로 통일 (Inspectable 사용 금지)
- 한국어 본문 + 영어 인용
- 폰트: Google Fonts에서 Noto Sans KR + JetBrains Mono
- 저장 경로: ~/Desktop/presentation/index.html

## 하지 말 것
- 기획 구조(슬라이드 순서/개수)를 변경하지 마
- 슬라이드를 임의로 추가/삭제하지 마
- Figure 없이 텍스트만 나열하지 마
- 과도한 애니메이션 금지 (fade 전환만 허용)
```
